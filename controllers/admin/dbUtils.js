/* const sqlite3 = require('sqlite3')

module.exports = {
    connect: new sqlite3.Database('./db/im-with-you-dev.db', err => {
        if (err)
            return console.error(err.message);
        console.log(`I'm with you ♥ | Connected to the sqlite DB!`);
    })
} */